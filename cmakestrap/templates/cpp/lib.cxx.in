module;

// unfortunately conan doesn't support modules yet, so we need to use global module fragment
#include <fmt/base.h>

export module ${project_name_safe};

export namespace ${project_name_safe}
{
    inline void hello()
    {
        fmt::println("Hello from ${project_name}!");
    }
}
